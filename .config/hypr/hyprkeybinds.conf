###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, Q, exec, uwsm app -- $TERMINAL
bind = $mainMod, C, killactive,
#bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, uwsm app -- $LAUNCHER
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, layoutmsg, togglesplit # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, focusmonitor, $MONITOR1 # ensures worksapce opens on a dedicatet monitor as binding via windowsrules is overridden
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, focusmonitor, $MONITOR2  # ensures worksapce opens on a dedicatet monitor as binding via windowsrules is overridden
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, focusmonitor, $MONITOR1  # ensures worksapce opens on a dedicatet monitor as binding via windowsrules is overridden
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, focusmonitor, $MONITOR2  # ensures worksapce opens on a dedicatet monitor as binding via windowsrules is overridden
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, focusmonitor, $MONITOR2  # ensures worksapce opens on a dedicatet monitor as binding via windowsrules is overridden
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Example special workspace (scratchpad)
bind = $mainMod, M, focusmonitor, $MONITOR2  # ensures worksapce opens on a dedicatet monitor as binding via windowsrules is overridden
bind = $mainMod, M, togglespecialworkspace, monitor
bind = $mainMod SHIFT, M, movetoworkspace, special:monitor

# Scroll through existing workspaces with mainMod + scroll
# bind = $mainMod, mouse_down, workspace, e+1
# bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
# XF86AudioMicMute,bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# Audio: Knob-Click (Übersetzt: XF86Tools) - Mic Mute Toggle
bindl = , XF86Tools, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# Audio: Knob-Dreh (Übersetzt: XF86Launch6) - Mic Lauter (Limit 100%)
bindel = , XF86Launch6, exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SOURCE@ 5%+

# Audio: Knob-Dreh (Übersetzt: XF86Launch5) - Mic Leiser
bindel = , XF86Launch5, exec, wpctl set-volume @DEFAULT_AUDIO_SOURCE@ 5%-

bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous


# Zoom
bind = $mainMod, mouse_down, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '')
bind = $mainMod, mouse_up, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '(.float * 0.9) | if . < 1 then 1 else . end')

binde = $mainMod, equal, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float * 1.1')
binde = $mainMod, minus, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '(.float * 0.9) | if . < 1 then 1 else . end')
binde = $mainMod, KP_ADD, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float * 1.1')
binde = $mainMod, KP_SUBTRACT, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '(.float * 0.9) | if . < 1 then 1 else . end')

bind = $mainMod SHIFT, mouse_up, exec, hyprctl -q keyword cursor:zoom_factor 1
bind = $mainMod SHIFT, mouse_down, exec, hyprctl -q keyword cursor:zoom_factor 1
bind = $mainMod SHIFT, minus, exec, hyprctl -q keyword cursor:zoom_factor 1
bind = $mainMod SHIFT, KP_SUBTRACT, exec, hyprctl -q keyword cursor:zoom_factor 1
bind = $mainMod SHIFT, 0, exec, hyprctl -q keyword cursor:zoom_factor 1

####################
### custom binds ###
####################
# toggle fullscreen
bind = $mainMod, F, fullscreen, 0
# browser
bind = $mainMod, B, exec, uwsm app -- $BROWSER
# hyprlock
bind = $mainMod, L, exec, loginctl lock-session

# yazi with win+e
bind = $mainMod, E, exec, uwsm app -- $TERMINAL zsh -i -c "$FILEMANAGER; exec zsh"

# clipboard history
bind = $mainMod CTRL, V, exec, cliphist list | fuzzel --dmenu | cliphist decode | wl-copy

# Diplay on/off
bind = $mainMod SHIFT, W, exec, hyprctl dispatch dpms off
bind = $mainMod, W, exec, hyprctl dispatch dpms on

# Fenster im Tiling-Baum verschieben (Topologischer Swap)
bind = $mainMod ALT, left, movewindow, l
bind = $mainMod ALT, right, movewindow, r
bind = $mainMod ALT, up, movewindow, u
bind = $mainMod ALT, down, movewindow, d

# screenshots (grimblast)
bind = $mainMod ALT, S, exec, uwsm app -- grimblast -f copy area -n

# notes
bind = $mainMod, N, exec, uwsm app -- $TERMINAL -e note
bind = $mainMod ALT, N, exec, uwsm app -- $TERMINAL -e note -ly
